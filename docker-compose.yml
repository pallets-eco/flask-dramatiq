version: "3"

services:
  rabbitmq:
    image: rabbitmq:3-alpine
    labels:
      com.dnsdock.alias: rabbitmq.flask-dramatiq.docker

  postgres:
    image: postgres:11-alpine
    labels:
      com.dnsdock.alias: postgres.flask-dramatiq.docker
